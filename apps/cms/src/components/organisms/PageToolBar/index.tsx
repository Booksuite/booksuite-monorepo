import { PageToolbarBackLink } from './PageToolbarBackLink'
import { PageToolbarRoot } from './PageToolbarRoot'
import { PageToolbarTitle } from './PageToolbarTitle'

export const PageToolbar = {
    Root: PageToolbarRoot,
    BackLink: PageToolbarBackLink,
    Title: PageToolbarTitle,
}
