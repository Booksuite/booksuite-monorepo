export type PaginationQueryInput = {
  /**
   * @description The page number
   * @type number
   */
  page: number
  /**
   * @description The number of items per page
   * @type number
   */
  itemsPerPage: number
}